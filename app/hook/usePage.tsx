"use client"
import {useEffect, useState} from "react";
import {useQuery} from "react-query";

export const usePage = ({uuid}: ClientProp) => {
    const [page, setPage] = useState<Page[]>([]);
    const [metadata, setMetadata] = useState<Metadata[]>();




    return {

    }
}